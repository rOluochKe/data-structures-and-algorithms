// Create an array to store words and display the contents both forward and backward

const wordsArray = ["apple", "banana", "cherry", "date"];

// Display contents forward
console.log("Words array forward:", wordsArray);

// Display contents backward
console.log("Words array backward:", wordsArray.slice().reverse());
